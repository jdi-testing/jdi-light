language: java
stages:
  - test
jobs:
  include:
    - stage: test
      name: Test stage
      script:
        - echo ------------------- TEST OUTPUT ----------------------
after_failure:
  - echo "Build was failed"
after_success:
  - echo "Build was successful"
after_script:
  - echo "End of build"
